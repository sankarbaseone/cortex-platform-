# ECD-015 — Claude Code Execution Manual
**Level:** 2 · The operating manual for the implementation engine. **Artifacts are the spec.**
- `artifacts/devex/CLAUDE.md` — complete, ships at repo root: authority hierarchy, never/always rules, build-verify loop, location map, stuck-protocol (GAP file, narrowest reading, gap-flagged PR — the ONLY sanctioned ambiguity channel).
- `artifacts/devex/README.md` — complete repo front door. `artifacts/devex/adr/` — ADR template + ADR-0001 (DI) and ADR-0002 (outbox) recording the two platform-wide decisions as discoverable ADRs.
- `artifacts/registries/build-order.yaml` — THE execution DAG: 7 sprints, 29 tasks, each with deps, exhaustive file scope, verify command, and Definition of Done. Zero tasks require a design decision; T-001→T-005 produce a green empty pipeline before any feature code.
- Registries completing the machine-readable contract: `services.yaml` (17 deployables + dataplane + web, ports/DBs/topics per service), `dependencies.yaml` (depguard source + version pins + forbidden list), `event-registry.yaml` (22 events → proto file → fixture path, fixture-required CI rule), `database-registry.yaml`, `environments.yaml` (dev→staging→prod→dr promotion + canary gates), `ownership.yaml` (CODEOWNERS source, solo-founder bootstrap noted), `ports.yaml`. Together with Run-1..3 registries (repository, api, metrics, alerts, dashboards, feature-flags) the manifest set from the Run-3 directive is COMPLETE.
- Execution protocol: work strictly in build-order sequence; a task is done only when its `verify` command passes and its DoD holds; every merge keeps main releasable (trunk rules RFC-011); prompts P1–P10 from RFC-012 remain the interaction templates.
